# 版本日志

## 0.1.0 - 2026-06-03

### 当前状态

- 已完成透明、无边框、始终置顶、可拖拽的 Electron 桌面宠物窗口。
- 已完成点击互动、本地文案气泡、宠物状态机，以及 `idle`、`happy`、`thinking`、`sleepy` 等状态。
- 已完成 `normal` / `game` 模式切换，右键菜单可进入设置、启动热气球射箭小游戏或退出应用。
- 已完成信息模块注册表，天气、Codex、即梦、macOS 日历提醒均通过独立模块接入。
- 已完成设置面板，可保存常用文案、气泡显示时间、长按触发时间、宠物尺寸、模块开关、闲置睡眠、天气、Codex、即梦和日历参数。
- 已完成资源动作映射系统，缺少正式图片时使用内置 SVG 占位，不会阻止应用启动。
- 已完成小游戏独立化，当前游戏 `balloon-shooter` 位于 `src/games/balloon-shooter`，可继续通过游戏注册表扩展新玩法。

### 第十阶段模块

- 天气模块通过 Open-Meteo 免 key 接口读取实时天气，并在普通点击时按间隔插入天气和建议文案。
- Codex 模块读取本机线程状态和可选 `~/.codex/desktop-pet-codex-status.json`，明确状态事件会触发跟随宠物的提示气泡。
- 即梦模块通过独立 Electron 页面读取网页生成状态，可显示登录、生成中、失败和完成提示，完成后可发送系统通知。
- 日历模块读取 macOS 日历中带备注的事件，接近开始时间时弹出气泡，并可发送系统通知。

### 交接提示

- 后续智能体应先阅读 `AGENTS.md` 和 `README.md`。
- 推荐下一步优化：天气定位、天气建议文案、Codex 状态识别精度、即梦页面状态识别、日历权限引导。
- 运行方式：

```bash
npm install
npm start
```

也可以直接运行：

```bash
./start.command
```

