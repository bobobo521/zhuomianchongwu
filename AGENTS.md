# 桌面宠物项目长期说明

这个项目是一个分阶段实现的 Electron 桌面宠物应用。后续任何对话都应先阅读本文件和 `README.md`，再继续实现。

## 最终目标

制作一个透明背景、无边框、始终置顶、可拖拽的桌面宠物。

用户可以点击宠物进行互动，宠物会切换动作并弹出对话气泡。气泡可以显示本地文案、天气信息、即梦生成进度等内容。

用户可以通过右键菜单进入小游戏模式：宠物坐上热气球上下浮动。小游戏中，宠物可以上下左右拖动，长按宠物蓄力，松开发射受重力影响的弓箭，射向屏幕另一侧的目标物。再次通过右键菜单可以退出小游戏模式，恢复普通宠物。

小游戏玩法参考 FC《猪小弟 / Pooyan》的形式：玩家角色在右侧吊篮/热气球区域活动，从右向左射箭，左侧竖向排列多个漂浮气球/箭靶目标。

## 技术要求

- 使用 Electron。
- 使用 JavaScript，不使用 TypeScript。
- 代码结构要清晰，适合后续扩展。
- 不要把所有逻辑写在 `renderer.js`。
- 每个功能模块拆分成独立文件。
- 美术资源不存在时使用 SVG 或 CSS 占位，不允许项目因此报错。
- 每完成一个阶段，都要告诉用户如何运行和测试。

## 分阶段路线

1. 基础 Electron 桌面宠物窗口。
2. 点击互动和对话气泡。
3. 宠物状态系统。
4. `normal` / `game` 模式系统，模式切换由右键菜单触发。
5. 右键菜单和信息模块系统。
6. 小游戏模式基础版，右键进入，热气球上下浮动。
7. 小游戏加入弓箭、目标物和碰撞检测。
8. 优化命中反馈、占位音效和宠物跟随气泡。
9. 加入设置面板。
10. 接入天气、本地即梦状态。

## 当前进度

已完成到第十阶段：

- 透明、无边框、始终置顶、可拖拽窗口。
- 点击互动和本地文案气泡。
- `idle`、`happy`、`thinking`、`sleepy` 状态系统。
- `normal` / `game` 模式系统，通过右键菜单切换模式。
- 右键菜单和信息模块注册表，天气/即梦先显示占位信息。
- 小游戏基础版，进入 game 模式时宠物切换为 `pet-balloon.png` 热气球形态；图片不存在时自动使用内置 SVG 占位图。
- game 模式下宠物持续进行带缓动感的上下浮动；退出 game 模式后会停止浮动、清除游戏状态，并恢复普通 `idle` 宠物。
- 小游戏加入弓箭、目标物和基础碰撞检测；进入 game 模式会扩展到屏幕范围，目标物生成在屏幕最左侧附近，宠物可在右侧拖动移动。鼠标在宠物以外区域时会穿透游戏层，仍可操作桌面其他窗口。点击宠物会从当前位置附近发射受重力影响的弓箭，长按蓄力后弓箭会更快、更直，命中后目标物播放反馈并刷新。
- 小游戏加入随机命中气泡和占位音效接口；命中目标后气泡跟随宠物位置显示，不固定在显示器最上方。不显示分数和 combo 系统。
- 设置面板已加入，可编辑常用文案、气泡显示时间、长按触发时间、宠物整体尺寸、模块开关和闲置睡眠时间，并保存到本地 JSON。
- 小游戏已独立化：桌宠只保留通用 `game` 窗口模式，具体玩法通过游戏注册表启动；当前热气球射箭玩法位于 `src/games/balloon-shooter`，游戏 ID 为 `balloon-shooter`，后续可以继续注册新的互动游戏。
- 第十阶段天气子模块已接入：天气通过 Open-Meteo 免 key 接口读取，设置面板可编辑城市和点击天气提醒间隔。间隔到达后的第一次普通点击显示实时天气，紧接着第二次点击显示“这个天气适合做什么”的建议。网络失败时显示友好兜底文案，不影响普通点击互动。
- 第十阶段即梦子模块已接入：设置面板可编辑即梦页面地址、检查间隔和完成通知开关。即梦模块通过独立 Electron 页面读取网页文字状态，可显示登录提醒、生成中进度、失败提示和完成提示；检测到生成完成后会弹出跟随宠物的成功气泡，并发送系统通知。默认即梦页面为 `https://jimeng.jianying.com/ai-tool/home?type=agentic&workspace=13853639902732`。
- macOS 日历提醒模块已接入：设置面板可开启日历提醒、调整检查间隔、提前提醒时间、未来查看范围和系统通知开关。日历模块会读取 macOS 日历中带备注的事件，接近开始时间时弹出跟随宠物的气泡并可发送系统通知；读不到日历权限时显示友好提示。
- 皮肤包 / 换肤系统已接入：设置面板可新建和切换多套皮肤，并为 normal 与 game 动作槽位上传 png/jpg/jpeg/webp 图片。上传资源会复制到 Electron `userData/skins/<skin-id>/`，不会覆盖项目 `src/assets`；皮肤不完整时允许启用，缺失动作会按回退规则使用同皮肤其他动作或默认动作。
- `src/assets` 已内置一套小猫外观默认皮肤，普通动作规格为 512 x 512 px 透明 PNG，小游戏动作和素材规格为 768 x 768 px 透明 PNG；可用 `node scripts/generate-default-cat-skin.mjs` 重新生成。

下一阶段可继续优化天气定位、建议文案、即梦页面状态识别和日历权限引导。

## 运行方式

```bash
cd /Users/bobobo/Desktop/Codex/桌宠
./start.command
```
